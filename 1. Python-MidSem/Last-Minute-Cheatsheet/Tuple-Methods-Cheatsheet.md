# 🔒 Tuple Methods Cheatsheet

## 🔍 Finding Elements (Only 2 Methods!)
- 🔢 **count(item)**: Count occurrences of value in tuple
  - `frequency = my_tuple.count(value)`
- 🎯 **index(item)**: Return index of first occurrence (error if not found)
  - `position = my_tuple.index(value)`

## 📏 Built-in Functions (Work with Tuples)
- 📊 **len()**: Get number of elements in tuple
  - `size = len(my_tuple)`
- 📈 **max()**: Get largest element in tuple
  - `largest = max(my_tuple)`
- 📉 **min()**: Get smallest element in tuple
  - `smallest = min(my_tuple)`
- ➕ **sum()**: Get sum of all numeric elements
  - `total = sum(my_tuple)`
- 🔍 **in**: Check if element exists in tuple
  - `exists = value in my_tuple`

## 🔄 Tuple Operations
- ➕ **Concatenation**: Join two tuples using +
  - `new_tuple = tuple1 + tuple2`
- ✖️ **Repetition**: Repeat tuple using *
  - `repeated = my_tuple * number`

#tuple-methods #python #cheatsheet