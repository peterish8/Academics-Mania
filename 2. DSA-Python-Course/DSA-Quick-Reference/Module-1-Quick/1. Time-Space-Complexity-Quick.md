# ⏱️ Time & Space Complexity - Quick Ref

## 🎯 Big O Cheatsheet

### **Time Complexities**
```python
O(1)     - Array access, hash lookup
O(log n) - Binary search, balanced tree
O(n)     - Single loop, linear search
O(n²)    - Nested loops, bubble sort
```

### **Space Complexities**
```python
O(1)     - Few variables
O(n)     - Extra array/hash of size n
```

---

## ⚡ **Power of Two Pattern**

**Problem**: Check if n is power of 2
**Trick**: `n > 0 and (n & (n-1)) == 0`

```python
def isPowerOfTwo(n):
    return n > 0 and (n & (n-1)) == 0
```

**Why**: Powers of 2 have exactly one bit set
- 8 = 1000, 7 = 0111 → 8 & 7 = 0000 ✓

---

## 🧠 **Memory Aids**

- **O(1)**: Instant access
- **O(log n)**: Halving each time  
- **O(n)**: Touch each element once
- **O(n²)**: Compare all pairs

Back to: [[README of DSA Quick Ref]]