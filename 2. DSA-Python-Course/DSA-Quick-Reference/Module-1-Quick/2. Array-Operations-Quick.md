# 🔢 Array Operations - Quick Ref

## 🎯 Core Patterns

### **Remove Duplicates (In-place)**
```python
def removeDuplicates(nums):
    j = 1
    for i in range(1, len(nums)):
        if nums[i] != nums[i-1]:
            nums[j] = nums[i]
            j += 1
    return j
```
**Key**: Two pointers, j tracks unique position

### **Rotate Array (3 Reversals)**
```python
def rotate(nums, k):
    k %= len(nums)
    reverse(nums, 0, len(nums)-1)    # Reverse all
    reverse(nums, 0, k-1)            # Reverse first k
    reverse(nums, k, len(nums)-1)    # Reverse rest
```

### **Merge Sorted Arrays**
```python
def merge(nums1, m, nums2, n):
    i, j, k = m-1, n-1, m+n-1
    while j >= 0:
        if i >= 0 and nums1[i] > nums2[j]:
            nums1[k] = nums1[i]
            i -= 1
        else:
            nums1[k] = nums2[j]
            j -= 1
        k -= 1
```
**Key**: Start from end to avoid overwriting

---

## 🧠 **Quick Tips**
- **In-place**: Use two pointers
- **Rotation**: 3 reversals trick
- **Merge**: Start from end

Back to: [[README of DSA Quick Ref]]