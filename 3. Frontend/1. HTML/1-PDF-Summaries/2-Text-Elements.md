# 📝 Text Elements - Summary

## 📏 **Headings (h1-h6)**
```html
<h1>Heading 1 - Largest</h1>
<h2>Heading 2</h2>
<h3>Heading 3</h3>
<h4>Heading 4</h4>
<h5>Heading 5</h5>
<h6>Heading 6 - Smallest</h6>
```

---

## 📄 **Paragraph**
```html
<p>This is a paragraph of text.</p>
```

---

## 💪 **Bold Text**
- `<strong>` - Important (semantic)
- `<b>` - Bold styling only

```html
<strong>Very important</strong>
<b>Bold text</b>
```

---

## 📐 **Italic Text**
- `<em>` - Emphasis (semantic)
- `<i>` - Italic styling only

```html
<em>Really interesting</em>
<i>Italic text</i>
```

---

## ✏️ **Other Text Elements**

**Deleted text:**
```html
<del>Removed text</del>
```

**Inserted text:**
```html
<ins>Added text</ins>
```

**Strikethrough:**
```html
<s>Not true anymore</s>
```

**Underlined:**
```html
<u>Underlined text</u>
```

---

## 🎯 **Quick Rules**
- Use `<strong>` and `<em>` for meaning
- Use `<b>` and `<i>` for styling only
- Use `<del>` and `<ins>` for edits
- Use `<s>` for outdated info