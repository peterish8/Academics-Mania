# 🎨 CSS Background Cont., Borders & Layout Properties

## 📌 Background Attachment
- `scroll` (default) → moves with content
- `fixed` → stays in place  
- `local` → scrolls with element's content

## 🔲 Border Styles
- `solid` → continuous line
- `dashed` → broken dashes
- `dotted` → small dots
- `double` → two parallel lines
- `none` → no border

## 🔄 Border Radius
- `border-radius: 15px` → rounded corners
- Creates circles, pills, rounded boxes

## 🌟 Box Shadow
- `box-shadow: offset-x offset-y blur-radius color`
- Adds depth and visual effects

## 📍 CSS Positions (5 Types)
1. **Static** → default, normal flow
2. **Relative** → positioned relative to itself
3. **Absolute** → positioned relative to nearest positioned ancestor
4. **Fixed** → positioned relative to viewport (stays on scroll)
5. **Sticky** → relative + fixed hybrid

## 📦 Display Property
- `block` → full width, new line
- `inline` → content width, same line
- `inline-block` → inline but accepts width/height
- `none` → completely hidden

## 👁️ Visibility Property
- `visible` → shown (default)
- `hidden` → invisible but takes space

## 🏗️ Z-Index
- Controls stacking order
- Higher number = on top
- Only works on positioned elements