# 📄 Sample Paper 2 - Frontend Development

> [!INFO] **Instructions**
> - Try to answer first before clicking the "Show Answer" toggle.
> - Focus on the **Explanation** and **Keywords** to build strong concepts.

---

## **Level 1: Beginner (HTML & CSS Basics)**

### 1. Which tag is used for the second-largest heading in HTML?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<h2>`
> **Explanation:** HTML provides 6 heading levels chosen by importance, from `<h1>` (most important) to `<h6>` (least important). `<h2>` is the second level.
> **Keywords:** Headings, Hierarchy, `<h2>`.

### 2. The `<span>` element is mainly used for:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Styling or grouping a part of text (Inline container).
> **Explanation:** `<span>` is a generic inline container used to apply styles or scripts to a small portion of text without breaking the flow.
> **Keywords:** Inline, Generic Container, Styling.

### 3. Which tag creates a bulleted list?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<ul>` (Unordered List)
> **Explanation:** `<ul>` creates a list where the items are marked with bullets. The children are `<li>` (List Item) tags.
> **Keywords:** Unordered List, Bullets.

### 4. The `<td>` tag is used inside:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<tr>` (Table Row)
> **Explanation:** `<td>` stands for "Table Data" and defines a standard cell within a table row (`<tr>`).
> **Keywords:** Table Data, Cell, Table Structure.

### 5. What does the charset meta attribute define?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The character encoding of the HTML document (e.g., `UTF-8`).
> **Explanation:** `<meta charset="UTF-8">` ensures that the browser displays special characters and symbols correctly.
> **Keywords:** Character Encoding, UTF-8.

### 6. Which attribute sets the destination URL of a link?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `href`
> **Explanation:** The `href` (Hypertext Reference) attribute in an `<a>` tag specifies the URL that the link goes to.
> **Keywords:** Hyperlinks, Anchor Tag, Destination.

### 7. Which element groups related form controls?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<fieldset>`
> **Explanation:** The `<fieldset>` tag allows you to group related elements in a form, often used with a `<legend>` to create a caption for the group.
> **Keywords:** Form Grouping, Accessibility.

### 8. Which semantic tag defines the main content area?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<main>`
> **Explanation:** The `<main>` tag specifies the dominant content of the `<body>`, excluding content repeated across pages like headers/footers.
> **Keywords:** Semantic HTML, Main Content.

### 9. Which tag creates a menu of selectable items?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<select>` (Dropdown list)
> **Explanation:** The `<select>` element is used to create a drop-down list. The specific options are defined by `<option>` tags.
> **Keywords:** Dropdown, Form, Selection.

### 10. Audio controls can be enabled using which attribute?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `controls`
> **Explanation:** Adding the `controls` boolean attribute to an `<audio>` tag displays the browser's default play, pause, and volume controls.
> **Keywords:** Audio, Media Controls.

### 11. Which attribute provides tooltip text for images?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `title`
> **Explanation:** The `title` attribute shows extra information (tooltip) when a user hovers over the element. Note: `alt` is for alternate text when the image fails to load.
> **Keywords:** Tooltip, Hover Info.

### 12. Which form attribute controls where the data is sent?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `action`
> **Explanation:** The `action` attribute in the `<form>` tag defines the URL where the form data is sent upon submission.
> **Keywords:** Form Submission, Data Handler.

### 13. Shortcut to inspect a webpage in Chrome is:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `Ctrl` + `Shift` + `I` (or `F12`)
> **Explanation:** This opens the Chrome Developer Tools, allowing you to inspect elements, debug JS, and view the console.
> **Keywords:** DevTools, Inspect Element.

### 14. Which input type ensures only numeric values are entered?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `type="number"`
> **Explanation:** `<input type="number">` provides built-in validation to accept only numbers and often includes spinner controls.
> **Keywords:** Input Types, Validation.

### 15. `<colspan>` is used inside which element?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<td>` or `<th>`
> **Explanation:** The `colspan` attribute defines the number of columns a cell should span (merge horizontally).
> **Keywords:** Table Merging, Spanning.

### 16. `<figure>` typically contains:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** An image and its caption.
> **Explanation:** `<figure>` is a semantic container for self-contained content, usually an `<img>` and a `<figcaption>` for the description.
> **Keywords:** Semantic Image, Caption.

### 17. Which element forces a line break inside text?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<br>`
> **Explanation:** The `<br>` tag inserts a single line break. It is an empty tag (no closing tag).
> **Keywords:** Line Break.

### 18. Which tag represents a self-contained piece of content like an article?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<article>`
> **Explanation:** The `<article>` element represents independent, self-contained content that makes sense on its own (e.g., blog post, news story).
> **Keywords:** Semantic HTML, Independent Content.

### 19. Which input type selects a date?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `type="date"`
> **Explanation:** `<input type="date">` creates a date picker interface in supported browsers.
> **Keywords:** Input Types, Date Picker.

### 20. Which element is used to highlight text without extra importance?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `<mark>`
> **Explanation:** The `<mark>` tag defines text that should be marked or highlighted (usually with a yellow background) for reference purposes.
> **Keywords:** Highlight, Marking.

### 21. Which selector targets elements by their ID?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `#` (The Hash symbol)
> **Explanation:** In CSS, you use `#idName` to select the element with that specific `id`. Example: `#header`.
> **Keywords:** ID Selector, CSS Syntax.

### 22. Which property changes the text color?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `color`
> **Explanation:** The `color` property sets the foreground color of text content. Note: `background-color` is for the background.
> **Keywords:** Text Styling, Color.

### 23. The default display value for `<span>` is:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `inline`
> **Explanation:** `<span>` allows other elements to sit next to it on the same line and only takes up as much width as necessary.
> **Keywords:** Display Property, Inline.

### 24. Which property sets space outside an element's border?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `margin`
> **Explanation:** `margin` creates space *outside* the border. `padding` creates space *inside* the border.
> **Keywords:** Box Model, Spacing.

### 25. The `rem` unit is based on:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The root element's font-size (`<html>`).
> **Explanation:** `rem` stands for "Root Em". If the root is 16px, `1rem` = 16px regardless of parent container sizes.
> **Keywords:** CSS Units, Root Relative.

### 26. Which property gives rounded corners?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `border-radius`
> **Explanation:** `border-radius` defines the radius of the element's corners, creating rounded edges.
> **Keywords:** Styling, Borders.

### 27. Which Bootstrap class makes text bold?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `.fw-bold` (Bootstrap 5)
> **Explanation:** `.fw-bold` (font-weight bold) is the utility class in Bootstrap 5 to set `font-weight: 700`.
> **Keywords:** Bootstrap Utility, Typography.

---

## **Level 2: Intermediate (CSS Layouts & JS Fundamentals)**

### 1. Flexbox aligns items along the main axis using:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `justify-content`
> **Explanation:** `justify-content` defines how the browser distributes space between and around content items along the main-axis of a flex container.
> **Keywords:** Flexbox, Alignment, Main Axis.

### 2. Which property defines the number of columns in a CSS grid?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `grid-template-columns`
> **Explanation:** This property defines the line names and track sizing functions of the grid columns. Example: `grid-template-columns: 1fr 1fr;` creates two equal columns.
> **Keywords:** CSS Grid, Layout.

### 3. Write a media query targeting screens wider than 600px.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `@media (min-width: 600px) { ... }`
> **Explanation:** `min-width: 600px` means the styles apply only if the viewport is 600px or larger (Desktop-first approach usually uses max-width, Mobile-first uses min-width).
> **Keywords:** Responsive Design, Media Queries.

### 4. Which property controls the duration of a transition?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `transition-duration`
> **Explanation:** It specifies how many seconds (s) or milliseconds (ms) a transition effect takes to complete.
> **Keywords:** CSS Transitions, Timing.

### 5. `@keyframes` are used to:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Define animations and their stages.
> **Explanation:** `@keyframes` controls the intermediate steps in a CSS animation sequence by defining styles for keyframes (points in the animation cycle).
> **Keywords:** Animation, Keyframes.

### 6. CSS custom properties (variables) begin with:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `--` (Double hyphen)
> **Explanation:** Example: `--main-color: blue;`. They are accessed using the `var()` function, e.g., `color: var(--main-color);`.
> **Keywords:** CSS Variables.

### 7. To repeat a background image use:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `background-repeat: repeat;`
> **Explanation:** This is the default behavior. You can also use `repeat-x`, `repeat-y`, or `no-repeat`.
> **Keywords:** Backgrounds, Styling.

### 8. Which property hides content without removing layout space?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `visibility: hidden`
> **Explanation:** The element is not visible, but it still affects the layout (occupies space). `display: none` removes it correctly from the document flow.
> **Keywords:** Visibility, Layout.

### 9. Which display mode arranges items vertically using Flexbox?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `flex-direction: column`
> **Explanation:** By default, flex items are in a row. Setting `flex-direction` to `column` stacks them vertically.
> **Keywords:** Flexbox, Direction.

### 10. `em` is relative to:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The font-size of the **parent** element (or current element).
> **Explanation:** `1em` is equal to the current font size. If used for font-size, it is relative to the parent's font size.
> **Keywords:** CSS Units, Relative.

### 11. Which property defines spacing between grid items?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `gap` (or `grid-gap`)
> **Explanation:** `gap` is a shorthand property for `row-gap` and `column-gap`.
> **Keywords:** Grid, Spacing.

### 12. `z-index` works only if which property is set?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `position` (to anything other than `static`)
> **Explanation:** `z-index` only affects elements that have `position` set to `relative`, `absolute`, `fixed`, or `sticky`.
> **Keywords:** Stacking Context, Positioning.

### 13. What triggers a CSS hover transition?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The `:hover` pseudo-class.
> **Explanation:** Styles defined under `selector:hover` are applied when the user's mouse pointer is over the element.
> **Keywords:** Interaction, Pseudo-classes.

### 14. Which keyword prevents reassignment of a variable?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `const`
> **Explanation:** Variables declared with `const` must be initialized at declaration and cannot be reassigned.
> **Keywords:** ES6, Variables, Immutability.

### 15. Which type is returned by `typeof []`?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `'object'`
> **Explanation:** Arrays are a special type of object in JavaScript. There is no specific 'array' type output for `typeof`.
> **Keywords:** Data Types, JS Quirks.

### 16. Which method converts a value to JSON format?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `JSON.stringify()`
> **Explanation:** It takes a JavaScript object or array and converts it into a JSON string.
> **Keywords:** JSON, Serialization.

### 17. DOM stands for:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Document Object Model.
> **Explanation:** It is the tree structure representation of the HTML document that JS can interact with.
> **Keywords:** DOM, Terminology.

### 18. What does the `this` keyword refer to inside a method?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The object that owns the method.
> **Explanation:** In a regular method call `obj.method()`, `this` refers to `obj`.
> **Keywords:** Scope, Context.

### 19. Which method listens to events on elements?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `addEventListener()`
> **Explanation:** It attaches an event handler function to an element (e.g., `btn.addEventListener('click', fn)`).
> **Keywords:** Events, DOM.

### 20. `localStorage` values are stored as:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Strings
> **Explanation:** even if you store numbers or booleans, `localStorage` converts them to strings. You must parse them when retrieving.
> **Keywords:** LocalStorage, Data Types.

### 21. Which loop guarantees at least one execution?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `do...while` loop
> **Explanation:** The block of code is executed once before the condition is tested.
> **Keywords:** Loops, Control Flow.

### 22. Which operator spreads elements of an array?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** The Spread Operator (`...`)
> **Explanation:** It expands an iterable (like an array) into individual elements. Example: `[...arr1, ...arr2]`.
> **Keywords:** ES6, Syntax.

### 23. What will `new Date()` return?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** A Date object representing the current date and time.
> **Explanation:** It grabs the system's current time.
> **Keywords:** Date Object.

### 24. Which method repeats a function after a fixed interval?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `setInterval()`
> **Explanation:** Calls a function repeatedly, with a fixed time delay between each call.
> **Keywords:** Timing, Async.

### 25. `Boolean("")` evaluates to:
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `false`
> **Explanation:** An empty string is a "falsy" value in JavaScript.
> **Keywords:** Type Coercion, Truthy/Falsy.

### 26. Which function converts a string "10" to a number?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `Number()` or `parseInt()`
> **Explanation:** `Number("10")` returns `10`. `parseInt("10")` also returns `10`.
> **Keywords:** Type Conversion.

### 27. What will `5 + "5"` output?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `"55"` (String)
> **Explanation:** Since one operand is a string, JavaScript treats `+` as concatenation, converting the number `5` to a string.
> **Keywords:** Coercion, Concatenation.

### 28. Which array method removes the first element?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `shift()`
> **Explanation:** `shift()` removes the first element and returns it. `pop()` removes the last.
> **Keywords:** Array Methods.

### 29. Which object provides mathematical constants?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `Math`
> **Explanation:** The `Math` object contains static properties and methods like `Math.PI` or `Math.max()`.
> **Keywords:** Built-in Objects, Math.

### 30. Which function is used for HTTP requests in JS?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `fetch()`
> **Explanation:** The Fetch API provides a JS interface for accessing and manipulating parts of the HTTP pipeline.
> **Keywords:** API, Async, Networking.

### 31. Which selector returns the first matching element from the DOM?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** `querySelector()`
> **Explanation:** `document.querySelector('.class')` returns the *first* element that matches. `querySelectorAll` returns all of them.
> **Keywords:** DOM Selection.

### 32. Which type of function takes another function as input?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Higher-Order Function
> **Explanation:** A function that takes another function as an argument (callback) or returns a function is a Higher-Order Function.
> **Keywords:** Functional Programming.

---

## **Level 3: Advanced (Problem Solving & Theory)**

### 1. Coding: Write a program that prints the cube of numbers from 1 to N using a while loop.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> function printCubes(N) {
>   let i = 1;
>   while (i <= N) {
>     console.log(i * i * i); // or Math.pow(i, 3)
>     i++;
>   }
> }
> ```

### 2. Coding: Write JS code to calculate and log subtraction, multiplication, and modulus of 12 and 7.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> let a = 12;
> let b = 7;
> console.log("Subtraction:", a - b);      // 5
> console.log("Multiplication:", a * b);   // 84
> console.log("Modulus:", a % b);          // 5
> ```

### 3. Coding: Print the sum of all even numbers between 1 and N.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> function sumEven(N) {
>   let sum = 0;
>   for (let i = 1; i <= N; i++) {
>     if (i % 2 === 0) {
>       sum += i;
>     }
>   }
>   console.log(sum);
> }
> ```

### 4. Coding: Given N, count numbers not divisible by 4 or 6.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> function countNums(N) {
>   let count = 0;
>   for (let i = 1; i <= N; i++) {
>     // 'OR' logic depends on interpretation. 
>     // Usually "not divisible by 4 OR 6" means if it's divisible by 4, skip. If divisible by 6, skip.
>     if (i % 4 !== 0 && i % 6 !== 0) {
>       count++;
>     }
>   }
>   console.log(count);
> }
> ```

### 5. Coding: Write a function that returns num raised to the power of 5.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> function powerFive(num) {
>   return num ** 5; // ES6 Exponentiation operator
>   // OR return Math.pow(num, 5);
> }
> ```

### 6. Coding: Given an array, create a new array of only numbers greater than 10.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> const nums = [5, 12, 8, 130, 44];
> const largeNums = nums.filter(num => num > 10);
> console.log(largeNums); // [12, 130, 44]
> ```

### 7. Coding: Use `forEach()` to print each element of an array with its index.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> const items = ['apple', 'banana', 'cherry'];
> items.forEach((item, index) => {
>   console.log(`${index}: ${item}`);
> });
> ```

### 8. Coding: Convert all names in an array to uppercase and display the result.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> const names = ["alice", "bob"];
> const upperNames = names.map(name => name.toUpperCase());
> console.log(upperNames); // ["ALICE", "BOB"]
> ```

### 9. Coding: Given an array of objects (book list), print title and author details to the DOM.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> const books = [
>   { title: "The Hobbit", author: "Tolkien" },
>   { title: "1984", author: "Orwell" }
> ];
> 
> books.forEach(book => {
>   const p = document.createElement('p');
>   p.textContent = `${book.title} by ${book.author}`;
>   document.body.appendChild(p);
> });
> ```

### 10. Debug: Correct this code to print the largest number: 
`let nums = [3, 10, 5];`
`console.log(Math.maximum(nums));`

> [!SUCCESS]- ✅ Click to Reveal Answer
> **Solution:**
> ```javascript
> let nums = [3, 10, 5];
> // Issues:
> // 1. Method is Math.max(), not Math.maximum()
> // 2. Math.max() takes arguments, not an array. Use spread operator.
> console.log(Math.max(...nums)); 
> ```

### 11. Explain the difference between block-level and inline elements with example tags.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:**
> - **Block-level**: Starts on a new line and takes full width available. (e.g., `<div>`, `<h1>`, `<p>`, `<ul>`).
> - **Inline**: Flows within text, does not start a new line, takes only needed width. (e.g., `<span>`, `<a>`, `<img>`, `<b>`).

### 12. What are CSS selectors? Name three types with examples.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Patterns used to select elements for styling.
> 1. **Element Selector**: `p { color: red; }`
> 2. **Class Selector**: `.btn { background: blue; }`
> 3. **ID Selector**: `#header { height: 100px; }`

### 13. What is the difference between margin and padding?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:**
> - **Margin**: Space *outside* the border (pushes other elements away).
> - **Padding**: Space *inside* the border (pushes content away from the border).

### 14. Describe execution context and call stack in JavaScript.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:**
> - **Execution Context**: The environment where JS runs (Variables, Scope, `this`).
> - **Call Stack**: The mechanism JS uses to track function calls. It follows LIFO (Last In, First Out)—when a function is called, it's added to the stack; when finished, it's removed.

### 15. What is a Promise? Explain with a small code example.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** A Promise is an object representing the eventual completion (or failure) of an asynchronous operation.
> ```javascript
> const p = new Promise((resolve) => resolve("Done!"));
> p.then(msg => console.log(msg));
> ```

### 16. Explain the purpose of semantic HTML with examples.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Semantic HTML uses tags that convey the *meaning* of the content, not just appearance. This helps SEO and Accessibility.
> - `<div>` (Non-semantic) vs `<header>` (Semantic: "This is a header").
> - `<b>` (Bold) vs `<strong>` (Important).

### 17. Why is responsiveness important? How does CSS media query help?
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Users access sites on various devices (phones, tablets, PCs). Responsiveness ensures the site looks good on all sizes. Media queries (`@media`) allow applying different CSS rules based on the device width (e.g., stacking columns on mobile).

### 18. Explain Flexbox in simple words with a use case.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Flexbox (Flexible Box Layout) is a 1-dimensional layout method for arranging items in rows or columns. It's great for alignment and distributing space. **Use case**: A navigation bar where items need to be spaced evenly across the screen.

### 19. What is DOM manipulation? Give an example.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** Using JavaScript to change the content, structure, or style of the HTML page.
> **Example**: `document.getElementById("demo").innerText = "Hello!";` changes text on the fly.

### 20. Difference between let, var, and const with examples.
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:**
> - **var**: Function scoped, can be redeclared (Avoid using).
> - **let**: Block scoped, can be updated.
> - **const**: Block scoped, cannot be updated/reassigned.

### 21. Arrange the order of JS execution model: Global Execution → Function Execution → Call Stack (True/False).
> [!SUCCESS]- ✅ Click to Reveal Answer
> **Answer:** **False** (Technically ambiguous phrasing, but the flow is):
> 1. Global Execution Context is created.
> 2. Pushed to Call Stack.
> 3. Function Invocation creates Function Execution Context.
> 4. Pushed to Call Stack.
